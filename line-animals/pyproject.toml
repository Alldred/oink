[project]
name = "line-animals"
version = "0.1.0"
description = "Local mflux line-drawing animals for the Oink Kindle dashboard"
requires-python = ">=3.11"
dependencies = [
    "numpy>=2.0.0",
    "pillow>=11.0.0",
]

[project.optional-dependencies]
local = [
    "mflux>=0.18.0",
]

[tool.uv]
package = false
